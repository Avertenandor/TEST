# 🗑️ Массовое удаление всех workflow runs

## Быстрый старт

### 1. Создайте GitHub Personal Access Token:

1. Откройте: https://github.com/settings/tokens
2. Нажмите **"Generate new token (classic)"**
3. Название: `Delete Workflows`
4. Права: выберите **`repo`** (все подпапки)
5. Нажмите **"Generate token"**
6. **Скопируйте токен сразу!** (он показывается только раз)

### 2. Запустите скрипт:

```powershell
cd "C:\Users\konfu\Desktop\Генезис 1"
.\scripts\delete-all-workflows.ps1 -Token "ваш_токен_здесь"
```

### 3. Дождитесь завершения:

Скрипт автоматически удалит все 193+ workflow runs.

## Альтернатива через GitHub CLI

Если установлен GitHub CLI:

```powershell
# Установить GitHub CLI (если нет)
winget install GitHub.cli

# Авторизоваться
gh auth login

# Посмотреть все runs
gh run list --limit 200

# Удалить все runs (потребуется подтверждение)
gh run list --limit 200 --json databaseId --jq '.[].databaseId' | ForEach-Object { gh run delete $_ }
```

## Важно

- ⚠️ Это действие необратимо
- ⚠️ Старые runs нельзя восстановить
- ✅ Новые workflow запускаться не будут (они уже удалены)

